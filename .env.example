# ==============================================================================
# PERSONAL GEMINI JOURNAL - ENVIRONMENT CONFIGURATION
# ==============================================================================
# IMPORTANT: This file contains configuration placeholders.
# NEVER commit production secrets or credentials to source control.

# --- GEMINI AI CONFIGURATION (Server-Side Only) ---
# Required for Gemini AI multi-turn reflections and insight analysis.
# Kept securely on the server; NEVER prefix with VITE_ or expose to the browser.
# In production, this can also be retrieved via Google Cloud Secret Manager.
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# Optional: Google Cloud Project ID if using Google Cloud Secret Manager
GCP_PROJECT_ID="perfect-crawler-507606-r4"

# Optional: Secret Name in Secret Manager (defaults to GEMINI_API_KEY)
GEMINI_SECRET_NAME="GEMINI_API_KEY"

# --- FIREBASE SERVER CONFIGURATION ---
# Optional: Project ID for Firebase Admin SDK token verification.
# Automatically loaded from firebase-applet-config.json if omitted.
FIREBASE_PROJECT_ID="perfect-crawler-507606-r4"

# --- APPLICATION HOST CONFIGURATION ---
# The URL where this applet is hosted (e.g. Cloud Run service URL).
APP_URL="https://ais-dev-v7vn35vakxvxwaw2njcjik-917634890493.asia-southeast1.run.app"
